#include "binary_trees.h"

/**
 * binary_tree_uncle - check tree
 * @node: the node
 * Return: the node
*/
binary_tree_t *binary_tree_uncle(binary_tree_t *node)
{
	if (!node)
	return (NULL);

	printf("hello");

	return (node);
}
