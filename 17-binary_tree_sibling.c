#include "binary_trees.h"

/**
 * binary_tree_sibling - check tree
 * @node: the node
 * Return: the node
*/
binary_tree_t *binary_tree_sibling(binary_tree_t *node)
{
	if (!node)
	return (0);

	printf("hello");

	return (node);
}
